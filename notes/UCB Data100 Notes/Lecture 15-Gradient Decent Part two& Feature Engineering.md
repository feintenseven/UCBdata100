# Lecture 15-Gradient Decent Part two& Feature Engineering

**epoch** 将所有数据计算一次，pass through all data points

**update** 更新一次theta

**mini-batch gradient decent** 当数据量极大时，可以分批（batch）计算，此时每批会进行update，但所有数据都进行过计算后才算一次epoch

![image.png](Lecture%2015-Gradient%20Decent%20Part%20two&%20Feature%20Engin/image.png)

**Stochastic Gradient Descent（随机梯度下降）**

one batch contain one sample 更新快但是震荡